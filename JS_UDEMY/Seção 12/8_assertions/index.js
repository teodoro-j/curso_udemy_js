let arr = [2,3,6,7,5,8];
let arr2 = [];

function iterarArrya(arr){
    if(arr.length == o){
        throw new Errpr("O array precisa ter pelo menos um elemento");
    } else {
        for(let i = 0; i < arr.length; i++){
            console.log(i);
        }
    }
}

iterarArrya(arr);
iterarArrya(arr2);